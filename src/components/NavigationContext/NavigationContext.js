import { DefaultValueModalContext } from '@/constants/navProviderConst';
import { createContext } from 'react';

const NavigationContext = createContext(DefaultValueModalContext);

export default NavigationContext;
